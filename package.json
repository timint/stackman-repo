{
  "name": "stackman-repo",
  "version": "1.0.0",
  "description": "Generates platform specific feeds (apps/*.json) of available versions for apache, php, etc.",
  "main": "index.ts",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "build": "bun run index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "typescript": "^5.9.3"
  },
  "type": "module"
}
